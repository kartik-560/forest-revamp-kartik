// This file is no longer needed as filter logic is handled in global-filters.blade.php
// Keeping file for backward compatibility but disabling the loader logic

document.addEventListener("DOMContentLoaded", () => {
    // Loader logic moved to global-filters.blade.php
    // This prevents conflicts with the new AJAX-aware filter system
});
